# Pinned to the versions the checked-in results were produced with.
# Python 3.14.6, macOS 24.6.0 (arm64), CPU only.
numpy==2.5.1
sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1
tokenizers==0.22.2
